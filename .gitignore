# Virtual environments
venv/
.venv/
env/

# Python bytecode
__pycache__/
*.py[cod]
*$py.class

# Model artifacts
*.pt
*.ckpt
*.onnx

# Data — never commit real datasets (keep .gitkeep placeholders)
data/**/*
!data/**/.gitkeep

# Evaluation outputs — track structure, not generated JSON (keep .gitkeep)
results/**/*.json
!results/**/.gitkeep

# Audit trail. Never committed: it names callers (hashed) and carries the
# benchmark metrics whose disclosure is half the reason /benchmark is gated.
results/audit.jsonl
results/**/*.jsonl

# anomalib's Engine creates a versioned run workspace, results/<Model>/<Dataset>/
# <category>/vN, on every fit. Our own artifacts go to results/checkpoints/.
results/*/
!results/checkpoints/

# Environment / secrets
.env

# Notebooks
notebooks/.ipynb_checkpoints/

# OS / editor cruft
.DS_Store
